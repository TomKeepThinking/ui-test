@props(['img' => '', 'title' => '', 'content' =>''])

<div class="col-span-12 sm:col-span-12 lg:col-span-6">
    <div class="w-full pt-64 lg:pt-work-lg bg-gray-300"></div>
    <h4>{{$title}}</h4>
    <p class="text-grey">{{$content}}</p>
</div>
